# While_Loop

In this code, we implement while loop. First, we ask the user to input a value. If the input is q, then we go to the if condition and exit the loop by using the break statement. Else if the user inputs anything other than the numbers specified, then we continue the loop after showing invalid number.