apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
# stringData takes PLAIN text and base64-encodes it for you.
# Use this instead of hand-encoding into `data:` - that is where the
# classic "echo adds a trailing newline" bug comes from.
stringData:
  DB_USER: "devops_admin"
  DB_PASSWORD: "S3cur3P@ssw0rd"
  DB_HOST: "mysql.default.svc.cluster.local"
