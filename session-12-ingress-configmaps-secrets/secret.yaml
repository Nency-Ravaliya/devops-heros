# Disposable homework values only; these are not credentials for any real service.
apiVersion: v1
kind: Secret
metadata: {name: homework-db, namespace: rudray-devops-hw}
type: Opaque
stringData:
  DB_USER: homework_user
  DB_PASSWORD: demo-only-not-a-real-password
  DB_NAME: homework_db
