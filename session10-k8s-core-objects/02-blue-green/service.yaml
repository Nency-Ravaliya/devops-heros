apiVersion: v1
kind: Service
metadata:
  name: app-blue-green-service
spec:
  type: NodePort
  selector:
    app: app-blue-green
    version: blue
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30020
