apiVersion: v1
kind: Pod

metadata:
  name: hello-pod

spec:
  restartPolicy: Never

  containers:
    - name: hello
      image: busybox:latest
      imagePullPolicy: IfNotPresent
      command: ["sh", "-c", "echo Hello Kubernetes"]
