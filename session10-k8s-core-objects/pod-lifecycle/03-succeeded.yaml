apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: task
      image: busybox:latest
      imagePullPolicy: IfNotPresent
      command: ["sh", "-c", "echo 'Task started'; sleep 2; echo 'Task completed successfully'; exit 0"]
