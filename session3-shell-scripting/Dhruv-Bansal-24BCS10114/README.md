# Session 3 - Shell Scripting

Dhruv Bansal - 24BCS10114

`script.sh` takes a name, roll number, and comment. It prints the date, hostname, current user, and logged-in users. It creates `sysinfo_output/result.log` and saves the process list in `sysinfo_output/process.log`.

```bash
bash script.sh
```
