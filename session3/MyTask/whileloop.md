# Bash Input Loop

This script repeatedly asks the user to enter a number.

- Entering `e` exits the loop.
- Non-numeric input displays an error message.
- Valid numbers are displayed back to the user.

The loop continues until the user chooses to exit.