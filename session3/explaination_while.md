The script continuously asks the user to enter a number or q to quit.
We understood how if, elif, break, and continue control the
flow of the script. The script exits when q is entered, accepts valid
numbers, and shows an error for invalid input.