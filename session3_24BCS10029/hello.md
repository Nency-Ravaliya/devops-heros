This script is basically a quick way to set up a folder and a log file.
It makes a directory called hello, goes inside, and creates app.log.
After adding a short message to the file, it reads it back to the screen.
