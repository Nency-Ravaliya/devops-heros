This is a very basic script showing how variables work in bash.
It stores a string, a name, a roll number, and a comment into different variables.
Then it prints everything out to the terminal to show how to use them.
